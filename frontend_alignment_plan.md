# План выравнивания фронтенда с `frontend_example`

## 1. Изучить пример
- [x] Просмотреть `frontend_example/App.tsx`, `components/`, `services/`, зафиксировать ключевые паттерны и компоненты.

## 2. Сопоставить функционал
- [x] Составить таблицу соответствия страниц и действий между текущим `frontend/` и `frontend_example`.

| `frontend_example` | Что есть сейчас | Пробелы/замечания |
| --- | --- | --- |
| `DealsView` с боковой панелью, карточками, модалками (`AddDealForm`, `AddQuoteForm`, `ClientDetails`) | Канбан + панель заметок/распознавания | Нет модалок добавления сделок/клиентов/квот; нет клиентских карточек внутри сделки |
| `ClientsView` (список + детали + формы Add/Edit) | Таблица + простая форма | Нет детального просмотра, фильтров, TTS, переключателей статуса |
| `PoliciesView` | отсутствует | Нужно добавить страницу полисов/привязать к сделкам |
| `PaymentsView` + `FinanceView` (мониторинг, AddPaymentForm, AddFinancialTransactionForm) | Страница Finances с формами платеж/доход/расход | Привести UI/логику к виджетам из примера, добавить транзакции |
| `TasksView` | Список без фильтров | В примере есть статусы, переключатели, владельцы |
| `SettingsView`, `UserProfileDropdown`, `ToggleSwitch`, `TTSButton` | Настройки (грубые карточки) | Добавить элементы управления профилем, озвучку, переключение темы |
| Модалки `AddClientForm`, `EditClientForm`, `AddPolicyForm`, `AddPaymentForm`, `AddFinancialTransactionForm` | Только простые формы на странице | Нужно перенести модальные формы и связи с данными |
| `MainLayout` (навбар, сайдбар, user info) | Наш Layout проще | Требуется перенести верхнюю панель, индикаторы, действия пользователя |

## 3. Общие компоненты и сервисы
- [x] Перенести/адаптировать базовые компоненты (layout, карточки, фильтры) из `frontend_example/components` в `src/components`. (Готовы `MainLayout`, `UserProfileDropdown`, `ToggleSwitch`, `TTSButton`, модалки добавления.)
- [x] Обновить слой взаимодействия с API, учитывая `frontend_example/services`. (Добавлены методы для сделок, пайплайнов, стадий, финансовых сущностей, распознавания документов.)

## 4. Страницы и маршруты
- [x] Перенастроить `src/App.tsx`: единый лэйаут, полноценные разделы (deals/clients/policies/payments/finances/settings).
- [x] Сделки: убрать «воронку», добавить фильтры, поиск, карточки и правую панель как в `DealsView`.
- [x] Клиенты: панель деталей, заметки и быстрые действия (портированы ранее).
- [x] Финансы/платежи: отдельные страницы для мониторинга, таблиц и форм.
- [x] Полисы: представление сделок в виде реестра полисов с поиском и статусами.
- [ ] Доработать Dashboard (виджеты, рекомендации, активности).
- [ ] Прокачать Tasks (фильтры, статусы, владельцы).
- [ ] Обновить Documents/Settings под единый визуальный язык.

## 5. Стили и конфигурация
- [ ] Привести тему/стили к эталону (цвета, spacing, шрифты).
- [ ] Синхронизировать зависимости и настройки (`package.json`, tsconfig, `.env`).

## 6. Тестирование и полировка
- [x] Прогнать `npm run build` (10.11) для валидации TS/Vite.
- [ ] Провести ручной проход по страницам, зафиксировать оставшиеся отличия от `frontend_example`.
