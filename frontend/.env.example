# Dev-mode frontend talks to the API through Vite proxy by default.
# For prod-like local docker compose the frontend bundle is built with LOCAL_VITE_API_URL from docker-compose.yml.
VITE_API_URL=/api/v1

# Used by Settings UI to build a clickable Telegram bot link.
VITE_TELEGRAM_BOT_USERNAME=
